import { describe, it, expect } from 'vitest';
import { parse } from '../pipeline';

describe('parse (pipeline)', () => {
  it('should be implemented', () => {
    // TODO: implement parse(), then fill in real end-to-end test cases
    expect(parse).toBeDefined();
  });
});
